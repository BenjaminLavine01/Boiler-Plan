# PostgreSQL Database URL (from Railway)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://username:password@localhost:5432/semester_planner

# Alternative: If Railway uses POSTGRESQL_URL instead
# POSTGRESQL_URL=postgresql://username:password@host:port/database

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend API URL (for local development)
REACT_APP_API_URL=http://localhost:5000
